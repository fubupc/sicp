;; at first. all wire would be zero.
;; If input is changed to 1 and 0. The output of first and-gate 
;; in half-adder would be still 0. so inverter would be still 0.
;; so sum would be still 0 which is wrong.
